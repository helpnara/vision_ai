# 비전 기반 표면 결함 탐지 프로젝트 설계 문서

> 목적: Claude(멀티모달/코딩 어시스턴트)를 활용하여 물건의 표면 결함(surface defect)을 탐지하는
> 과제를 수행하기 위한 **사전 준비 사항**, **설계 과정**, **수정 이력**을 한 곳에서 관리한다.

- 문서 버전: v0.3
- 최초 작성일: 2026-07-22
- 최종 수정일: 2026-07-30
- 상태: 🟢 진행 중 — **1단계(데이터 수집) 구현 완료**, 2단계(라벨링) 착수 예정
- 데이터 정책: **회사(사내) 데이터는 사용하지 않음.** 오픈 데이터셋 + 직접 촬영 이미지만 사용
- 구현 형태: **Streamlit 단일 앱**에서 4단계를 순차적으로 구현
- 저장소 브랜치: `claude/vision-surface-defect-detection-j16xhj`

---

## 0. 확인 필요 항목 (Open Decisions)

> ✅ 확정 / 🟡 방향만 확정 / ⬜ 미정.
> 결정이 바뀌면 해당 섹션과 [수정 이력](#5-수정-이력-revision-history)을 함께 갱신한다.

| # | 결정 항목 | 결정 내용 / 잠정 가정 | 영향 범위 | 상태 |
|---|-----------|---------------------|-----------|------|
| D1 | 검사 대상 물건/제품 종류 | **일상 생활 속 물건.** 구체 품목은 사용할 오픈 데이터셋 카테고리에 맞춰 확정 | 결함 유형 정의 | 🟡 방향 확정 / 세부 미정 |
| D2 | Claude 활용 형태 | 하이브리드(전용 모델 1차 + Claude 2차 판정/설명) | 3단계 아키텍처 | 🟡 잠정 |
| D3 | 운영 환경 | **Streamlit 앱 기반 오프라인/배치.** 실시간 인라인은 범위 외 | 성능 요구사항 | ✅ 확정 |
| D4 | 데이터 확보 방식 | **오픈 데이터셋 기반.** 사내 데이터 미사용, 직접 촬영 이미지는 보조 검증용 | 1단계 전체 | ✅ 확정 |
| D5 | 정상/불량 판정 기준 및 허용 오탐률 | 미정 — 사용할 데이터셋의 라벨 정의를 따르고, 임계값은 3단계에서 실험적으로 결정 | 평가 지표/합격 기준 | ⬜ 미정 |
| D6 | 촬영 환경 통제 | 오픈 데이터셋은 이미 통제된 환경. 직접 촬영분은 통제 불가로 간주하고 품질 점검으로 걸러냄 | 데이터 품질·전처리 | ✅ 확정 |
| D7 | 사용할 오픈 데이터셋 | MVTec AD 우선 검토(비상업 라이선스 확인 필요), VisA 차선 | 학습 데이터 | ⬜ 미정 |

---

## 1. 프로젝트 개요

### 1.1 배경 및 목표
- **배경**: 사람 육안 검사에 의존하던 표면 결함 검사를 비전 기술로 자동화·표준화하여 검사 일관성과 처리량을 높인다.
- **검사 대상 방향**: **일상 생활에서 접할 수 있는 물건의 표면 결함**을 탐지하는 모델을 만든다.
  (예시 후보: 스마트폰/노트북 등 기기 표면 스크래치, 벽·가구 흠집, 과일·채소 표면 흠, 도자기·식기 파손, 의류 얼룩 등 — 구체 품목은 추후 확정)
- **데이터 제약**: 회사(사내) 데이터는 활용에 무리가 있어 사용하지 않으며, 일상에서 촬영 가능한 이미지와 공개 데이터셋만 사용한다.
- **목표**: 입력 이미지에 대해 (1) 결함 유무 판정, (2) 결함 유형 분류, (3) 결함 위치(영역) 표시를 자동으로 수행한다.
- **성공 기준(초안)**: 아래 [4.2 평가 지표](#42-평가-지표) 참조. 정량 목표는 D5 확정 후 수치화한다.

### 1.2 범위 (Scope)
- **포함**: 데이터 수집/전처리, 결함 정의, 탐지 파이프라인 설계·구현, 평가, 리포팅.
- **제외(확정)**: 실제 생산라인 하드웨어(카메라/PLC) 통합, 대량 실시간 서빙 인프라. 운영 형태는 Streamlit 앱 기반 오프라인/배치로 한다 (**D3**).

### 1.3 용어 정의
| 용어 | 설명 |
|------|------|
| 결함(Defect) | 정상 표면 대비 허용 기준을 벗어난 이상(스크래치, 찍힘, 이물, 얼룩 등) |
| 오탐(False Positive) | 정상을 결함으로 잘못 판정 |
| 미탐(False Negative) | 결함을 정상으로 놓침 (품질 리스크가 가장 큼) |
| ROI | 검사 관심 영역(Region of Interest) |

---

## 2. 사전 준비 사항 (Preparation Checklist)

### 2.1 문제 정의
- [x] 검사 대상 범위 확정: **일상 생활 속 물건의 표면 결함** (구체 품목은 D7 확정 시 따라 결정)
- [x] 탐지할 결함 유형 목록 확정 — 10종 표준 분류 ([3.6](#36-결함-유형-분류-체계-확정))
- [x] 미탐(FN) 우선 정책 확정 — 결함을 놓치는 쪽이 리스크가 크므로 재현율을 최우선 지표로 둔다
- [ ] 각 결함의 "합격/불합격" 기준 정의 (크기·개수·위치 임계값) (**D5**) → 3단계에서 실험적으로 결정

### 2.2 데이터
- [x] **회사 데이터 미사용 원칙 확정** — 오픈 데이터셋 + 직접 촬영 이미지만 사용 (**D4**)
- [x] 데이터 확보 방식 확정: **오픈 데이터셋 기반**, 직접 촬영분은 보조 검증용
- [x] 오픈 데이터셋 후보 조사 및 비교 (7종, 라이선스·일상 물건 적합도 포함 → 1단계 카탈로그)
- [x] 데이터 인덱스(manifest) 스키마 정의 — 출처/카테고리/분할/라벨/품질 지표 단일 파일 관리
- [x] 중복 이미지 제거 방식 확정 (파일 내용 sha1 기준)
- [x] 클래스 불균형 자동 점검 (정상:결함 비율 5:1 이상이면 경고)
- [x] 데이터 저장 방식 확정: `data/`는 git 추적 제외, 코드·설정만 저장소에 둠
- [ ] 사용할 데이터셋 최종 선정 및 라이선스 확인 (**D7**)
- [ ] 라벨링 규칙·도구 결정 (분류 라벨 / 바운딩박스 / 픽셀 마스크 중 필요 수준) → 2단계
- [ ] 학습/검증/테스트 분할 정책 수립 (데이터 누수 방지) → 2단계

### 2.3 촬영/환경
- [x] 촬영 환경 통제 전제 정리 (**D6**): 오픈 데이터셋은 통제된 환경, 직접 촬영분은 비통제로 간주
- [x] 비통제 이미지 대응책 구현: 흐림(Laplacian 분산)·노출·해상도 자동 점검 후 경고 표시
- [ ] 직접 촬영 시 카메라 해상도·거리·각도 규격 정의 (직접 촬영을 본격화할 때)

### 2.4 기술/환경 셋업
- [x] 개발 언어/런타임: Python 3.11
- [x] UI: **Streamlit 단일 앱** (4단계를 페이지로 분리)
- [x] 핵심 라이브러리 확정(1단계): OpenCV, NumPy, pandas, Pillow, scikit-learn
- [x] 재현성: 합성 데이터 시드 고정, 의존성 명시(`requirements.txt` / `pyproject.toml`)
- [x] 테스트: pytest — 코어 로직을 UI와 분리해 검증 가능하게 구성
- [ ] Claude API 연동: `anthropic` SDK 멀티모달 — API 키/비용 한도 확인 → 3단계
- [ ] 딥러닝 스택(PyTorch 등) 추가 → 3단계
- [ ] 실험 관리 방식 결정 (CSV/Markdown → 필요 시 MLflow) → 3단계

### 2.5 산출물
- [x] 1단계 수집 파이프라인 코드 + 테스트
- [x] 프로젝트 사용 가이드 (`README.md`)
- [x] 본 설계 문서의 지속적 갱신
- [ ] 2~4단계 구현
- [ ] 평가 리포트(지표 + 오탐/미탐 샘플 시각화) → 3단계

---

## 3. 설계 과정 (Design)

### 3.1 접근 방식 후보 비교

| 접근 | 개요 | 장점 | 단점 | 적합 상황 |
|------|------|------|------|-----------|
| A. 고전 CV | 임계값/에지/모폴로지 기반 규칙 | 빠름, 데이터 少, 해석 쉬움 | 조건 변화에 취약 | 결함 패턴이 단순·일정 |
| B. 지도학습 탐지(YOLO 등) | 라벨 이미지로 결함 검출 학습 | 위치+유형 동시 검출, 정확 | 라벨 데이터 필요 | 결함 종류 명확·데이터 확보 |
| C. 이상탐지(anomaly detection) | 정상만 학습, 이탈을 결함으로 | 결함 라벨 거의 불필요 | 유형 분류는 약함 | 결함 샘플 희소 |
| D. Claude 비전 API 직접 판정 | 이미지를 Claude에 전달해 판정/설명 | 라벨 少, 프롬프트로 빠른 시작, 자연어 설명 | 처리량/비용/미세결함 한계 | 소량·애매 케이스·설명 필요 |
| E. 하이브리드(B/C + D) | 전용 모델 1차 스크리닝 + Claude 2차 판정·설명 | 정확도와 설명력 동시 | 구성 복잡 | 실무 균형 (**잠정 채택**) |

> **잠정 채택(가정)**: **E. 하이브리드** — 데이터 상황에 따라 초기엔 C(이상탐지) 또는 D(Claude 직접)로 시작하고,
> 데이터가 쌓이면 B(지도학습)를 추가한다. D2 확정 시 확정.

### 3.2 파이프라인 (초안)

```
[이미지 입력]
    → [전처리: ROI 크롭 / 정규화 / 조명 보정 / 리사이즈]
    → [1차 탐지: 이상탐지 or 전용 검출 모델]  ──(결함 의심 영역)──┐
    → [2차 판정: Claude 비전 API로 유형 분류 + 근거 설명]        │
    → [후처리: 임계값 적용 / 결함 집계 / 합격·불합격 판정]        │
    → [출력: 판정 결과 + 시각화(박스/마스크) + 리포트]  ◀────────┘
```

### 3.3 Claude 연동 설계 (초안)
- **입력**: 원본 또는 크롭된 결함 후보 이미지 + 구조화된 프롬프트(결함 유형 목록, 판정 기준).
- **출력 형식**: JSON 스키마 강제(예: `{"defect": bool, "type": "scratch|dent|...", "severity": 1-5, "reason": "..."}`).
- **역할**:
  - 소량 데이터 단계에서 1차 판정기.
  - 하이브리드 단계에서 전용 모델의 애매한 결과를 재판정하고 **사람이 읽을 수 있는 근거**를 제공.
- **주의**: 미세·저대비 결함은 한계가 있으므로 임계값·해상도 실험 필요. 실시간 처리를 목표로 하지 않으므로(**D3**) 지연시간보다 호출 비용 관리가 관건이다.

### 3.4 구현 아키텍처 — Streamlit 4단계 앱

전체 파이프라인을 **하나의 Streamlit 앱**에서 단계별 페이지로 구현하고, 순차적으로 완성한다.

| 단계 | 페이지 | 주요 기능 | 상태 |
|------|--------|-----------|------|
| 1 | 데이터 수집 (입력) | 오픈 데이터셋 카탈로그, 로컬 폴더 임포트, 이미지 업로드, 합성 샘플 생성, 품질 점검 | ✅ 완료 |
| 2 | 라벨링 | 라벨 검수 큐, 폴더 라벨 검증, 결함 유형 매핑, ROI 표시, 층화 분할 | ⬜ 예정 |
| 3 | 모델 개발 · 평가 | 베이스라인 → 이상탐지 → Claude 2차 판정, 지표·오탐/미탐 리포트 | ⬜ 예정 |
| 4 | 사후 운영관리 (MLOps) | 모델 레지스트리, 추론 로그, 데이터/성능 드리프트 감시, 재학습 트리거 | ⬜ 예정 |

**설계 원칙**
- **UI와 코어 로직 분리**: 판정·수집 로직은 `src/vision_ai/`에 두고 Streamlit 페이지는 표시만 담당한다.
  로직을 UI 없이 pytest로 검증할 수 있어야 한다.
- **manifest를 단일 진실 원천으로**: `data/manifest.csv` 하나가 "어떤 이미지가 어디서 왔고 어떤
  라벨·품질 상태인지"를 담는다. 2단계 이후 모든 단계가 이 파일을 입력으로 쓴다.
- **원본 파일 불변**: 폴더 임포트는 파일을 이동·복사하지 않고 경로만 인덱싱한다.
- **외부 다운로드 없이도 전 과정 실행 가능**: 오픈 데이터셋은 용량이 크고 약관 동의가 필요하므로,
  MVTec AD와 동일한 구조의 합성 데이터를 생성해 파이프라인 배선을 먼저 검증한다.
  (합성 데이터는 성능 근거로 쓰지 않는다.)

### 3.5 디렉터리 구조 (구현 반영)
```
vision_ai/
├── app.py                     # Streamlit 진입점 (네비게이션)
├── app_pages/                 # 단계별 화면
│   ├── home.py                #   진행 현황 대시보드
│   ├── p1_ingest.py           #   1단계: 데이터 수집          ✅
│   ├── p2_labeling.py         #   2단계: 라벨링               ⬜
│   ├── p3_modeling.py         #   3단계: 모델 개발·평가        ⬜
│   └── p4_operations.py       #   4단계: 운영관리(MLOps)      ⬜
├── src/vision_ai/             # 코어 로직 (UI와 분리)
│   ├── config.py              #   경로, 라벨/결함 유형 체계
│   ├── storage.py             #   manifest 읽기/쓰기, 중복 제거
│   ├── datasets.py            #   오픈 데이터셋 카탈로그, 폴더 구조 파서
│   ├── quality.py             #   이미지 품질 점검
│   └── ingest.py              #   수집 로직 + 합성 데이터 생성
├── tests/                     # pytest (31개)
├── docs/
│   └── surface-defect-detection-plan.md   # 본 문서
├── data/                      # (gitignore) 이미지, manifest.csv
├── artifacts/                 # (gitignore) 모델, 리포트
├── requirements.txt
├── pyproject.toml
└── README.md
```

### 3.6 결함 유형 분류 체계 (확정)

일상 물건 기준으로 정의한다. 오픈 데이터셋의 고유 유형명(예: MVTec AD의 `broken_large`)은
2단계 라벨링에서 아래 표준 유형으로 정규화한다.

| 키 | 설명 | 키 | 설명 |
|----|------|----|------|
| `scratch` | 스크래치 / 긁힘 | `contamination` | 이물 부착 |
| `dent` | 찍힘 / 눌림 | `broken` | 파손 / 깨짐 |
| `crack` | 균열 / 갈라짐 | `hole` | 구멍 / 찢김 |
| `stain` | 얼룩 / 오염 | `deformation` | 변형 / 휘어짐 |
| `discoloration` | 변색 / 색편차 | `other` | 기타 |

---

## 4. 평가 및 검증 (Evaluation)

### 4.1 검증 원칙
- 학습에 쓰지 않은 **독립 테스트셋**으로 평가.
- 미탐(FN) 샘플은 개별 리뷰(품질 리스크 직결).

### 4.2 평가 지표
| 지표 | 의미 | 비고 |
|------|------|------|
| Recall(재현율) | 실제 결함 중 잡아낸 비율 | **미탐 최소화가 핵심이면 최우선** |
| Precision(정밀도) | 결함이라 판정한 것 중 실제 비율 | 오탐 비용과 연결 |
| F1 / AUROC | 종합 성능 | 임계값 튜닝 참고 |
| 처리 시간 | 장당 추론 시간 | 배치 운영(**D3**)이므로 목표치는 느슨함. Claude 호출 비용과 함께 관리 |
| 오탐/미탐 비용 | 현업 관점 손익 | D5 확정 후 가중 |

> 정량 합격 기준(예: Recall ≥ 0.98)은 **D5 확정 후** 이 표에 수치로 확정한다.

---

## 5. 수정 이력 (Revision History)

| 버전 | 날짜 | 작성/수정자 | 변경 내용 |
|------|------|-------------|-----------|
| v0.1 | 2026-07-22 | Claude Code | 최초 초안 작성. 개요·사전 준비·설계·평가 골격 정의, 확인 필요 항목(D1~D6) 명시 |
| v0.2 | 2026-07-22 | Claude Code | 검사 대상을 **일상 생활 속 물건의 표면 결함**으로 방향 확정. **회사 데이터 미사용** 원칙 명시(일상 촬영·공개 데이터만 사용). D1·D4 상태 갱신 |
| v0.3 | 2026-07-30 | Claude Code | **오픈 데이터셋 기반 + Streamlit 4단계 앱**으로 구현 방식 확정. 1단계(데이터 수집) 구현 완료. D3·D4·D6 확정, D7(사용 데이터셋) 신규 추가. 구현 아키텍처(3.4)·디렉터리 구조(3.5)·결함 유형 체계(3.6) 추가, 사전 준비 체크리스트 진척 반영 |

> 이후 변경 시 이 표에 한 줄씩 추가한다. (버전 규칙: 큰 방향 변경 시 소수 첫째 자리 증가, 세부 보완 시 둘째 자리 증가)

### 5.1 v0.3 구현 상세 (1단계)

**구현한 것**
- 오픈 데이터셋 카탈로그 7종 (MVTec AD, VisA, MPDD, BTAD, Magnetic Tile, DAGM 2007, NEU-DET) —
  라이선스·상업적 이용 가능 여부·일상 물건 적합도·폴더 구조를 함께 정리
- 폴더 구조 자동 해석기 2종 (`mvtec`: `<카테고리>/<train|test>/<good|결함유형>/`, `flat`: `<클래스폴더>/이미지`)
  — 등록 전 추론 결과 미리보기 제공, `ground_truth` 마스크는 기본 제외
- 이미지 업로드 (직접 촬영분), sha1 기반 중복 제거
- 합성 샘플 생성기 — 4종 표면(목재·직물·도장금속·도자기) × 4종 결함(scratch/dent/crack/stain),
  MVTec AD와 동일한 폴더 구조로 출력
- 품질 점검 — 흐림(Laplacian 분산), 노출(평균 밝기), 해상도 부족 자동 판정
- 수집 현황 대시보드 — 분포 차트, 클래스 불균형 경고, 품질 경고 목록, 미리보기, manifest 내려받기
- pytest 31개 (경로 파서, 중복 제거, 품질 점검, 합성 생성, 결함 국소성 회귀 테스트)

**개발 중 수정한 사항**
| 문제 | 원인 | 조치 |
|------|------|------|
| 합성 `dent`·`stain` 결함이 이미지 전체를 뭉개 선명도 지표가 함께 떨어짐 | 결함을 전체 이미지 블렌딩(`addWeighted`)으로 합성 | 페더링한 알파 마스크로 **국소 합성**하도록 변경. 회귀 테스트 추가(변경 픽셀 25% 미만) |
| 합성 데이터 36건 중 20건이 과다 노출 경고 | 도자기 표면 기본 밝기(235)가 임계값(215)을 초과 | 기본 밝기를 208로 조정. 표면별 품질 통과 테스트 추가 |
| 탭 안에서 조건 미충족 시 페이지 전체가 렌더링 중단 | `st.stop()`이 스크립트 전체를 중단시킴 | 탭 함수 내에서는 `return`으로 변경 |
| `use_container_width` 지원 종료 파라미터 사용 | Streamlit에서 2025-12-31 이후 제거 예정 | `width="stretch"`로 교체, 최소 버전 1.60으로 명시 |

---

## 6. 다음 단계 (Next Steps)

### 즉시
1. **사용할 오픈 데이터셋 선정 (D7)** — MVTec AD가 첫 후보(일상 품목 다수 + 결함 마스크 제공)이나
   비상업 라이선스이므로 사용 목적 확인이 먼저다. 상업적 활용 여지를 남기려면 VisA(CC BY 4.0)가 대안.
2. 선정한 데이터셋을 내려받아 **1단계 로컬 폴더 임포트로 실제 데이터 등록** — 폴더 구조 해석이
   실제 데이터에서 의도대로 동작하는지 확인한다. (그 전에는 합성 샘플로 파이프라인 확인 가능)

### 2단계 (라벨링) 착수 시
3. 오픈 데이터셋의 폴더 라벨을 표준 결함 유형(3.6)으로 정규화하는 매핑 정의.
4. 층화 분할(train/val/test) 구현 — 카테고리·라벨 비율 유지, 데이터 누수 방지.
5. 직접 촬영 이미지 라벨링 UI.

### 이후
6. 3단계에서 베이스라인 → 이상탐지 순으로 학습하고, Claude 비전 2차 판정을 붙인다(D2 확정).
7. 판정 임계값 실험 결과로 **정량 합격 기준(D5)** 을 4.2 표에 수치로 확정한다.
