# 1~4단계 실행에 필요한 의존성
#
# opencv는 반드시 -headless 판을 쓴다. 일반 opencv-python은 libGL.so.1을 요구해서
# 데스크톱 라이브러리가 없는 배포 컨테이너에서 import 단계부터 실패한다.
# (-headless를 쓰므로 packages.txt로 apt 패키지를 따로 깔 필요가 없다.)
#
# 하한만 지정해 두었다. 배포를 고정하고 싶으면 아래 "검증된 조합"으로 == 고정할 것.
#   streamlit 1.60.0 / pandas 3.0.5 / numpy 2.4.6 / pillow 12.3.0 /
#   opencv-python-headless 5.0.0.93 / scikit-learn 1.9.0 / joblib 1.5.3 /
#   anthropic 0.120.2  (Python 3.11.15에서 전체 테스트 통과)
streamlit>=1.60
pandas>=2.2
numpy>=1.26
pillow>=10.0
opencv-python-headless>=4.9
scikit-learn>=1.4
joblib>=1.3

# 3단계 Claude 2차 판정 (없어도 나머지 기능은 동작한다)
anthropic>=0.60

# 4단계에서 필요해지면 추가
# torch>=2.2
# anomalib>=1.0
