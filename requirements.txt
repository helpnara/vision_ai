# 1~3단계 실행에 필요한 의존성
streamlit>=1.60
pandas>=2.2
numpy>=1.26
pillow>=10.0
opencv-python-headless>=4.9
scikit-learn>=1.4
joblib>=1.3

# 3단계 Claude 2차 판정 (없어도 나머지 기능은 동작한다)
anthropic>=0.60

# 4단계에서 필요해지면 추가
# torch>=2.2
# anomalib>=1.0
