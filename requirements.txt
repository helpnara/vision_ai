# 1단계(데이터 수집) 실행에 필요한 최소 의존성
streamlit>=1.60
pandas>=2.2
numpy>=1.26
pillow>=10.0
opencv-python-headless>=4.9
scikit-learn>=1.4

# 3단계(모델 개발)에서 추가 예정 — 필요할 때 주석 해제
# torch>=2.2
# torchvision>=0.17
# anthropic>=0.40        # Claude 비전 2차 판정
# anomalib>=1.0          # 이상탐지 모델 모음
